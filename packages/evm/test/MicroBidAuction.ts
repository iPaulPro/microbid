import { expect } from "chai";
import { ethers } from "hardhat";
import { MicroBidAuction } from "../typechain-types";

describe("MicroBidAuction", function () {
  // We define a fixture to reuse the same setup in every test.
  // let yourContract: MicroBidAuction;
  // before(async () => {
  //   const [owner] = await ethers.getSigners();
  //   const factory = await ethers.getContractFactory("MicroBidAuction");
  //   yourContract = (await factory.deploy(owner.address)) as MicroBidAuction;
  //   await yourContract.waitForDeployment();
  // });
  //
  // describe("Deployment", function () {
  // });
});
